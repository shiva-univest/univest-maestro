appId: com.univest.capp.uat
tags:
  - auth
  - login
---
# Launch the app
- runFlow: ../common/launch.yaml

# Check if login screen is visible — skip if already logged in
- runFlow:
    when:
      visible: "Enter Mobile Number"
    file: login_steps.yaml
